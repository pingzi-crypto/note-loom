console.log(
  "Ledger draft audit skipped: built-in time audit classification rules were retired from runtime."
);
console.log(
  "Time range parsing now keeps only structural facts such as start, end, duration, and note."
);
